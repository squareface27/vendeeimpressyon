{% extends 'base.html.twig' %}

{% block title %}ArticlesEntity{% endblock %}

{% block body %}
    <h1>ArticlesEntity</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ articles_entity.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ articles_entity.name }}</td>
            </tr>
            <tr>
                <th>Unitprice</th>
                <td>{{ articles_entity.unitprice }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_articles_entity_index') }}">back to list</a>

    <a href="{{ path('app_articles_entity_edit', {'id': articles_entity.id}) }}">edit</a>

    {{ include('articles_entity/_delete_form.html.twig') }}
{% endblock %}
